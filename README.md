# changes-96

Папку `src` положить в корень проекта с заменой.

Этот архив возвращает цвета плашек к состоянию changes-94.

Ставить можно как поверх changes-94, так и поверх changes-95: в обоих случаях
получится одно и то же верное состояние. Если changes-95 еще не поставлен, его
ставить не нужно вовсе.

## Что сделано

Плотные плашки из changes-95 отменены. Вернулись прежние цвета:

- автор `#ffeabd`, нажатие `#ebd8ae`
- команда `#c9e9dc`, нажатие `#b5d4c8`
- компания `#cfe0f8`, нажатие `#bccde4`

Абрикосового варианта в файлах нет нигде.

Шарики в списках карточек не менялись и остались насыщенными: у автора золото
от `#ffcb5c` к `#e09600`, у команды зелень, у компании синий.

## Файлы

```
src/components/FindAccordion.tsx
src/components/JoinPicker.tsx
src/components/PlansTable.tsx
```
