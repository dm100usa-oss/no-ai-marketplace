import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Listing Policy" };

export default function Page() {
  return (
    <StubPage
      title="Listing Policy"
      intro="Rules for listing a profile on No AI Marketplace."
      note="Legal text is inserted by the owner later. Placeholder page."
    />
  );
}
