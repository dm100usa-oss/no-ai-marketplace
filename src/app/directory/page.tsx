import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Directory" };

export default function Page() {
  return (
    <StubPage
      title="Directory"
      intro="All active human-made creators and companies. Browse by direction and category, or search."
      note="The full catalog with search and filters is built on stage 3."
    />
  );
}
