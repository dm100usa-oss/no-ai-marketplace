import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Contact" };

export default function Page() {
  return (
    <StubPage
      title="Contact"
      intro="Get in touch with the No AI Marketplace team."
      note="The contact form is added on stage 4."
    />
  );
}
