import { StubPage } from "@/components/StubPage";

export const metadata = { title: "Directions" };

export default function Page() {
  return (
    <StubPage
      title="Directions"
      intro="The major directions of the catalog. Each opens into its categories."
      note="Direction pages are generated from data on stage 2."
    />
  );
}
