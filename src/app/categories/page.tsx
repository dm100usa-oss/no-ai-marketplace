import { StubPage } from "@/components/StubPage";

export const metadata = { title: "All categories" };

export default function Page() {
  return (
    <StubPage
      title="All categories"
      intro="Every category across all directions, each with its own indexable page."
      note="Categories are generated from data on stage 2."
    />
  );
}
